<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bike.mapper.ParkingAreaMapper">

    <sql id="baseColumns">
        id, name, center_lng, center_lat, radius, create_time
    </sql>

    <select id="findList" resultType="ParkingArea">
        SELECT <include refid="baseColumns"/> FROM parking_area
        <where>
            <if test="name != null and name != ''">AND name LIKE CONCAT('%', #{name}, '%')</if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="findById" resultType="ParkingArea">
        SELECT <include refid="baseColumns"/> FROM parking_area WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO parking_area (name, center_lng, center_lat, radius)
        VALUES (#{name}, #{centerLng}, #{centerLat}, #{radius})
    </insert>

    <update id="update">
        UPDATE parking_area
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="centerLng != null">center_lng = #{centerLng},</if>
            <if test="centerLat != null">center_lat = #{centerLat},</if>
            <if test="radius != null">radius = #{radius},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM parking_area WHERE id = #{id}
    </delete>

</mapper>
