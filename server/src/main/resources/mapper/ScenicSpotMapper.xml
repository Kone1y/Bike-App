<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bike.mapper.ScenicSpotMapper">

    <sql id="baseColumns">
        id, name, description, lng, lat, cover_image, create_time
    </sql>

    <select id="findList" resultType="ScenicSpot">
        SELECT <include refid="baseColumns"/> FROM scenic_spot
        <where>
            <if test="name != null and name != ''">AND name LIKE CONCAT('%', #{name}, '%')</if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="findById" resultType="ScenicSpot">
        SELECT <include refid="baseColumns"/> FROM scenic_spot WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO scenic_spot (name, description, lng, lat, cover_image)
        VALUES (#{name}, #{description}, #{lng}, #{lat}, #{coverImage})
    </insert>

    <update id="update">
        UPDATE scenic_spot
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="description != null">description = #{description},</if>
            <if test="lng != null">lng = #{lng},</if>
            <if test="lat != null">lat = #{lat},</if>
            <if test="coverImage != null">cover_image = #{coverImage},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM scenic_spot WHERE id = #{id}
    </delete>

</mapper>
